---
- name : SUDO
  hosts: tag_Name_RedHat
  remote_user: ec2-user
  # remote_user: user
  become: true
  
  tasks:
    - lineinfile:
        path: /etc/sudoers
        state: present
        regexp: '^%sudo'
        line: '%sudo ALL=(ALL) NOPASSWD: ALL'
        validate: 'visudo -cf %s'
